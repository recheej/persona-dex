<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseAppTheme">
        <!-- Customize your theme here. -->
        <item name="statsBackground">?colorPrimary</item>
        <item name="pageBackground">@color/darkThemeSurface</item>
        <item name="dialogPageBackground">@color/personaGrey</item>
    </style>


    <style name="PersonaStatsTheme" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?statsBackground</item>
        <item name="android:orientation">vertical</item>
    </style>
</resources>